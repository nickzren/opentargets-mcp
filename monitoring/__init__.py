"""Operational monitoring for the published package and its registry listing.

Not part of the distributed package: `[tool.setuptools.packages.find]` only
looks under `src/`, so nothing here ships to PyPI.
"""
